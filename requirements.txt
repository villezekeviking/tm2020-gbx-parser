# Core dependencies: None (uses Python stdlib: struct, zlib, io, math, hashlib, os, datetime)

# Optional: python-lzo>=1.14 (for replay .Gbx files with LZO-compressed body)
# Note: .Ghost.Gbx files use zlib compression (no LZO needed)

# Microsoft Fabric / PySpark dependencies (for Lakehouse notebooks)
pyspark>=3.3.0
delta-spark>=2.3.0
pandas>=2.0.0
pyarrow>=12.0.0
